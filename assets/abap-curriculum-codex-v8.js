/*
 * ABAP Curriculum Codex v8 add-on — load AFTER abap-curriculum-codex-v7.js.
 *
 * The "전체화면" (fullscreen) toggle now lives in the shared Stage 7 shell
 * (assets/stage7-shell.js) so every documentation page exposes it from the top
 * bar instead of a floating button. This add-on keeps only the v8 layout overlay
 * (wider content + full-bleed fullscreen styling in abap-curriculum-codex-v8.css);
 * no extra JavaScript behaviour is required here.
 */
